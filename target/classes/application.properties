# H2 Console Configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Datasource Configuration
spring.datasource.url=jdbc:h2:mem:faq;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA and Hibernate Configuration
# Automatically update the schema when the application starts
#spring.jpa.hibernate.ddl-auto=update
# Define the dialect to use H2 syntax
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# Show SQL queries in logs
#spring.jpa.show-sql=true
# Format SQL for better readability in logs
#spring.jpa.properties.hibernate.format_sql=true
# Enable SQL comments (optional, useful for debugging)
#spring.jpa.properties.hibernate.use_sql_comments=true

# Script Initialization (Ensures scripts are executed in the correct order)
# Run schema and data SQL scripts (adjust as needed for production)
#spring.sql.init.mode=always
#spring.sql.init.schema-locations=classpath:schema.sql
#spring.sql.init.data-locations=classpath:data.sql

# Server Configuration
#server.port=8080

# Logging Configuration
# Logs SQL statements executed by Hibernate
#logging.level.org.hibernate.SQL=DEBUG
# Logs binding parameters for SQL statements (useful for debugging)
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# Server Configuration
server.port=8080

# Logging Configuration (opcional)
logging.level.org.springframework=INFO
